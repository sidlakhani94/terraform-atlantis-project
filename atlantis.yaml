version: 3
projects:
- dir: .
  name: production
  workflow: production
  autoplan:
    enabled: false
  apply_requirements: [approved]
workflows:
  production:
   plan:
     steps:
     - init
     - run: ./run_test.sh
     - plan
