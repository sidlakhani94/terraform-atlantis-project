version: 3
projects:
- dir: .
  name: production
  workflow: production
  terraform_version: v0.14.8
  autoplan:
    enabled: false
  apply_requirements: [approved]
workflows:
  production:
   plan:
     steps:
      - init
      - run: ./run_test.sh
      - plan
